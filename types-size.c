#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]) {
    int i = 31;
    printf("size of i = %lu\n", sizeof(i));
    char v = 'a';
    printf("size of i = %lu\n", sizeof(v));
    return (EXIT_SUCCESS);
}
